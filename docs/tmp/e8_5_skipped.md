# E8-5 スキップ記録（夜間バッチ・2026-08-18〜19）

「判断に迷う項目はスキップして理由を記録＝止まらないこと優先」の指示に基づく記録。
番号は `docs/tmp/e8_gap_matrix.md` の行番号。

## casts（/casts）

- **casts#4 基本情報編集（かな・管理メモ・cast_update RPC）**: スキップ。裁定書 §4-5 のとおり
  **採用だが段未割当（要確認）**のまま＝mig（casts 2列＋cast_update 新設）を伴うため
  「mig 不要」の本バッチ条件外。相談役の段割当を待つ。
- **casts#6 実績・勤怠タブの週次推移・遅刻/欠勤 KPI**: 縮小実装（次回シフト1行のみ追加）。
  週次推移チャートは既存 RPC の再形と言っても週境界・分母の定義が裁定されておらず、
  夜間バッチで定義を発明しない。残りは E8-6 か次段で。
- **casts#9 新規登録の入力項目（写真・プラン・招待のモーダル集約）**: スキップ。
  登録フローの再編は M 級で、現行の「登録→招待→写真は別操作」分離は機微情報分離の
  設計と絡む（trial_hire 経路も同じフォームを共有）。誤操作リスク＞見た目の利得と判断。
- **casts#3 の LINE 連携表示**: T3 後送り（裁定どおり・対象外）。

## payroll（/payroll）

- **payroll#2 の「出勤日数」列**: スキップ（列自体は復元したが日数のみ）。preview breakdown（PayResult）に
  wDays 相当が無く wHours のみ＝日数は attendance の別取得＋「出勤した日」定義の適用が要る。
  時間（wHours）・総支給・控除計の3列は復元済み。
- **payroll#4 の「状態」フィルタ**: スキップ（名前検索のみ実装）。プレビュー行に per-cast の
  支払状態が無い（支払状態は PaymentPanel＝確定後の区画にある）。行単位の状態を出すには
  payment_records の cast 別集計をプレビュー表に持ち込むことになり、参考値（未確定）の表に
  確定後データを混ぜる形になるため見送り。
- **payroll#7 給与を調整モーダル**: 対象外（裁定書 §4-4 の裁定漏れ＝要確認のまま）。
- **payroll#9 ヘッダの一次アクション集約**: 縮小（ボタンの複製はしない）。#1 のステッパーに
  「次: …」の誘導文言を載せる形で到達性を確保した。同一操作のボタンを2箇所に置くと
  無効化条件の二重管理（腐る危険）が生じるため、操作ボタンは従来どおり各セクション1箇所。
- **payroll#6 LINE 明細公開**: T3 後送り（裁定どおり・対象外）。

## audit（/audit）

- **audit#7 記録の詳細（リスク度・操作理由・要確認レビュー）**: スキップ。裁定は「列がある範囲で」＝
  audit_logs に risk/reason/review 列が無く、既存列（IP・店舗・before→after 差分）は表示済み。
  列追加は audit#3 不採用（現行で監査要件は足りる）と同じ理由で作らない。
- **audit#4 現金照合ビュー／#6 データ出力**: 後送り（裁定どおり・T5 依存含む）。

## 席・営業時間（/master）

- **席#4 席の追加項目（伝票表示名・管理メモ・POS表示）**: スキップ。seats への列追加＝mig を
  伴うため本バッチ条件（mig 不要）外。マトリクスの注記どおり段冒頭の mig 要否確認から。
- **席#5 の ↑↓並べ替え**: スキップ（検索・種別フィルタは実装）。並べ替え専用 RPC が無く、
  ↑↓は set_seat 2連続呼びの非原子（片方失敗で順序が壊れ audit も2行）になる。現行の
  表示順数値入力で同じ結果が得られるため、reorder RPC（0077/0081 型）の新設裁定待ち。
- **席#1 独立ページ vs ハブ**: 作業なし（実装のハブ→view 切替で機能は既に存在＝マトリクスどおり）。
- **営業時間#8 の特別営業日件数 KPI**: 対象外（#7 特別営業日が後送りのため分子が存在しない）。

## staff・システム（/staff + /master システム）

- **staff#1 1ページ4タブ vs 2画面3パネル**: スキップ。M 級の IA 再編で、PIN（/master システム）と
  名簿（/staff）の分離は権限境界（owner 限定パネルの同居回避）と絡む。夜間バッチで
  ページ統合を発明しない。
- **staff#5 端末の最終アクセス・IP・オンライン**: 対象外（裁定書 §4-2＝要確認のまま。
  レジ端末のみ kiosk_sessions.last_seen_at で近似可という判定材料は提出済み・裁定待ち）。
- **staff#2 PIN 設定済み数 KPI／staff#7 権限データ化**: 不採用（裁定どおり・作らない）。
- **staff#3 PIN ポリシー**: 後送り（裁定どおり）。

## 既存プルダウンの発見列挙（置換対象候補・今回は列挙のみ）

- casts-board.tsx: 指名ランク割当 select（詳細 comp タブ）／RegisterForm の配属店 select・区分 select
