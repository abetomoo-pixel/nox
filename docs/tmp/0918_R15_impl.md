# 0918_R15_impl — 裁定274（R15）client 便の実装メモ（2026-09-18）

## S-0 台帳本文（逐語）

- 裁定274（本便で確定・Agoora「推奨で」）: 「[裁定274 R15] シフト画面を 5→3 タブ(今日／作る＝[作成|仮]／確定＝[承認待ち|確定シフト])・語は 3 語固定(公開/承認/確定)。status 拡張・既読と同意の分離は第2期。client 1 便。」
- 裁定253 E（L3307）: 「R15 シフト画面の情報設計の見直し（「確定」の語が計画の公開・行の confirmed・確定シフトタブで別の意味に使われ、5 タブを 3 往復する導線になっている。タブ構成の畳み込みを別裁定で設計）。」
- 前提調査＝docs/tmp/0918_R15_pre.md（分岐 1-(b)＋2-(a)＋3-(a)＋4-(a) が採られた）。

## S-0 suite の pin（逐語 grep）

- `grep -n '承認待ち|確定シフト|仮シフト|シフト作成|"queue"|"roster"|"calendar"|"build"|"today"' scripts/verify-nox-shift-*.ts scripts/verify-nox-staff-shift.ts scripts/verify-nox-late.ts`＝ヒット 0（shift-modal のヘッダコメント「シフト作成モーダル v6」のみ・assert ではない）。
- shift-board.tsx を readFileSync で読む suite＝0（late.ts の suite はコメントで 817 行を参照するのみ）。
- ⇒ **張り替え点 0**。新設 suite verify:nox-shift-tabs（14・純関数＋逐語結線）を f0 59 段目に連結（package.json）。

## S-1 実装

- lib/nox/shift/tabs.ts（新設・純関数）: SHIFT_VIEWS（今日／作る／確定）・VIEW_TABS（作る＝[build 作成|calendar 仮シフト]・確定＝[queue 承認待ち|roster 確定シフト]・today は 1 択）・viewOfTab・tabOfView（中のキーは据え置き・外なら先頭）・LEGACY_SHIFT_TABS。
- shift-board.tsx: 旧 5 タブの `<nav className="nox-subnav">` を 3 タブに置換し、その直下に `.nox-seg` の 2 択（today では出さない）。state 型 `useState<"today"|"calendar"|"build"|"queue"|"roster">` と setTab の呼び出し 5 箇所（648 calendar／837 queue／1579 roster／1599 queue／nav）・各パネルの `tab === "…"` 分岐は不変＝深リンク維持。承認待ちの件数バッジ＝「確定」タブに丸数字（従来と同形）＋seg「承認待ち」に素の数字。
- suite: verify:nox-shift-tabs 14（st(1)〜(5)・逆テスト＝viewOfTab の calendar を confirm にする→st(2-2)／(4-1) 赤・戻して緑）。

## S-2 語の統一（置換 N＝15・13 種・表示文言のみ）

原則＝動詞（行を承認する操作）だけ「承認」。状態名（confirmed＝「確定」・未確定・確定シフト・確定時間・確定人時・確定者）・監査ラベル（146 行 shift_confirm_bulk:「確定」）・DB status 値・RPC 名は不変。

| # | 行（適用前） | 旧 | 新 |
|---|---|---|---|
| 1 | 458 confirmShift のトースト | 確定に失敗／確定しました | 承認に失敗／承認しました |
| 2 | 546 confirmOne のトースト | 確定に失敗／確定しました | 承認に失敗／承認しました |
| 3 | 508 公開後の案内 | シフトの確定は「承認待ち」タブから | シフトの承認は「確定」タブの承認待ちから |
| 4 | 531 一括の確認ダイアログ | 件をまとめて確定しますか？ | 件をまとめて承認しますか？ |
| 5 | 536 一括の途中失敗 | 件確定・残りは再試行してください | 件承認・残りは再試行してください |
| 6 | 539 一括の完了 | 件を確定しました | 件を承認しました |
| 7 | 836 CTA の title | 承認待ちタブで内容を確認してから確定します | 承認待ちで内容を確認してから承認します |
| 8 | 837 CTA | 確定する（n 件） | 承認する（n 件） |
| 9 | 1108 今日タブの行ボタン | 確定 | 承認 |
| 10 | 1379 一括ボタンの title | まとめて確定します（62件ずつ…） | まとめて承認します（62件ずつ…） |
| 11 | 1381 一括ボタン | 件を一括確定 | 件を一括承認 |
| 12・13 | 1507／1519 承認待ちの行ボタン（2 箇所） | 確定 | 承認 |
| 14・15 | 1834／2129 カレンダーの注記（2 箇所） | （予定・確認待ちは「承認待ち」タブ） | （予定・確認待ちは「確定」タブの承認待ち） |

残る「確定」91→81（全て状態名・タブ名・列名・コメント）。

## S-3 目視（dev 3200・owner-a）

- 375px: 3 タブ（今日／作る／確定）が 1 行・「作る」で seg［作成|仮シフト］・「確定」で seg［承認待ち|確定シフト］・seg の切替と 3 タブ往復で state が保たれる（tabOfView の据え置き）・body 横幅不動。
- 1280px: 同上（サイドバー付き）。
- cast（/mine）: 表示不変（本便の変更は (manage)/shift のみ）。
