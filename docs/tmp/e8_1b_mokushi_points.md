# E8-1b/1c 目視ポイント一覧（Agoora 実機確認用・2026-08-18 再発行＋E8-1c 追補）

前提: dev（http://localhost:3200）・demo-manager でログイン・CLUB NOX。
E8-1 の目視ポイントに **F1〜F6 の是正分を加えた再発行版**。push はこの目視 OK 後（E8-1 分と合わせて1回）。

## F1 人数単位（person 制）

1. マスタ → 料金設定 → 時間料金設定: 単位セグが「卓単位｜人数単位」表記＋下に説明文
   （卓単位＝1伝票につき…／人数単位＝伝票の人数×料金…人数変更に自動で追随）。
2. **CLUB NOX は人数単位に恒久切替済み**（ご指示どおり）。
3. レジで開卓（人数2）→ 会計タブ: セット料金行が **「¥5,000 × 2名 ¥10,000」** の形（×N名 表記）。
4. 伝票ヘッダの人数±を 2→3 に変更 → 会計タブ再入場でセット行が ×3名 に追随。

## F2 指名フロー1本化

1. 伝票 → 指名・席タブ: nom_type プルダウンが消え、**本指名｜場内｜同伴｜フリーのセグ**に。
2. キャスト選択は CastPicker（検索＋写真グリッド）。選ぶと按分の重みにも同時反映。
3. 「本指名料を付ける（＋按分）」を押す → 明細に指名料行＋
   **「れいな に本指名料 ¥3,000 を追加し、按分にも反映しました」** の成功メッセージ（緑）。
   ¥0 だった場合は ¥0 警告が出る（旧: 無言で ¥0 行が積まれるバグ→是正済み）。
4. 按分の重み微調整は **「按分の重みを微調整（れいな＝100%）」の折りたたみ**の中＋「按分を保存」。

## F3 席操作の視覚化

1. 指名・席タブの席カード: ボタン2つ「相席を追加」「席を移動」。
2. どちらを押しても **空席タイルのグリッドモーダル**が開き、タイルをタップで確定（旧 select 廃止）。

## F4 ナビゲーション

1. 伝票詳細を開いた状態でサイドバー「レジ」を再クリック → **フロア（卓一覧）へ戻る**。
2. 「← フロア」ボタンが大型化（太字 15px・押しやすいサイズ）。

## F5 会計グループ付け替え（mig0091）

1. 会計タブの明細行: グループバッジが **「会計A ▾」ボタン**に。タップでモーダル。
2. モーダルで「会計B」を選ぶ → 行が会計Bへ移動しバッジ表示が追随（合計は不変）。
3. 「＋会計を分けてそこへ」で新グループ作成と同時移動。
4. 動かせない行（自動時間料金・割引行・入金済みグループ・close 済み）はバッジが淡色で反応しない。
   ※自動時間料金行は DB 側でも 'time line' で拒否（会計A固定）。

## F6 会計完了モーダル＋簡易領収書

1. 入金 → 会計完了（close）→ **「会計完了」モーダル**: 合計・お釣り（最後の現金入金の釣銭）。
2. 簡易領収書: 宛名（空欄可）＋但し書き（既定「お飲食代として」）→「領収書を印刷 / PDF」で
   ブラウザ印刷（白地黒字・宛名/金額/但し/日付/店名）。画面には印字内容は出ない（印刷時のみ）。
3. レシート再印刷ボタンは**プリンタ設定オンの店のみ**表示（CLUB NOX は未設定のため非表示＝仕様）。
4. 分割領収書・QR・採番は後送りのまま（E8-1b 指示どおり）。

## E8-1c 分割領収書＋人数注記

1. close 後モーダルの簡易領収書が**分割ダイアログ**に: 「1〜10枚に分割できます。均等割り
   （最後の1枚は残額を自動計算）＝発行合計は会計金額と必ず一致します」の説明＋枚数ボタン 1〜10枚。
2. 3枚を選ぶ→均等割り（例 ¥2,200 → 734/733/733）。最終枚は readonly＋「残額を自動計算」表記。
3. 1枚目の金額を手修正（例 1000）→最終枚が残額へ自動再計算（例 467）・
   下部に「3枚・合計 ¥2,200（会計金額と一致）」。不一致になる操作は構造上できない
   （万一 0 円以下になる編集はクランプ・印刷ボタンは一致時のみ有効）。
4. 宛名・但し書きは**枚数分 個別入力**（空欄＝上様／ご飲食代として）。
5. 「領収書を印刷 / PDF（N枚）」→ブラウザ印刷で**1枚=1ページ**（page-break）・各枚に宛名/金額/但し/日付/店名。
6. 発行番号・QR のボタンは**出ない**（R2 送りのまま）。
7. 伝票ヘッダの人数±の下に注記（person 制の店のみ表示・モード別の正確な文言）:
   - 自動店:「※セット中の人数変更はセット料金に反映されます。延長中の変更は現在は
     全時間に反映されます（延長時点からの加算は今後の改修で対応）」
   - 手動店:「※セット中の人数変更はセット料金に反映されます。追加済みの延長行には
     反映されません（延長は追加時点の人数で確定します）」
   ※卓単位の店は人数が料金に効かないため注記を出しません（嘘をつかない）。

## E8-1d 指名種別の可視化（追補）

1. 指名・席タブのキャストチップ: 「着卓中」バッジが**種別付き**に（本指名=gold／場内=gold2／
   同伴・フリー=グレー）。判定＝課金行の凍結 fee_kind 優先（hon_shimei＞jonai_shimei＞dohan）→
   按分のみのキャストは伝票の指名種別。課金行だけ・按分だけのどちらでも表示が破綻しない。
2. 按分微調整の行に種別併記（例:「れいな（本指名）50%」「えま（場内）50%」）。
3. 実機確認済み: 本指名1（れいな）＋場内1（えま）で両バッジ・色分け・併記を確認→伝票 void で復元。

## E8-5 横断段（夜間バッチ・追補）

1. **/casts**: 先頭に KPI 4枚（在籍/体入中/未招待/今月出勤者）。ツールバー下にランク絞り込みチップ。
   カードにランク名・プラン名の副次行。詳細の基本タブに「次回シフト」。待遇・バックタブに
   割当プランの実値（保証時給・本/場内/同伴バック・上書きバッジ）。
2. **/payroll**: run バーに 3段ステッパー（集計・確認→確定→支払＋次アクション）。確定サマリ下に
   未支払と前月比。プレビュー表に名前検索＋時間/総支給/控除計の列（広い画面で表示）。
   行タップで個別内訳がインライン展開。支払記録に「メモ（任意）」欄。
3. **/audit**: KPI 4枚（本日の記録/取消・巻き戻し/機微アクセス/権限・端末）＋同名の4ビュータブ。
4. **/master 席ビュー**: KPI 4枚＋席名検索＋種別セグ。**営業時間ビュー**: KPI 4枚（営業日/定休日/未設定/週の営業時間）。
5. **/master システム**: キオスク端末カードに「端末設定の履歴（直近10件）」。レシート・プリンタに
   **実プレビュー**（入力がサンプルレシートに即時反映）。機密・税務に「機微情報の閲覧履歴」（owner のみ）。
6. **/customers**: 登録・編集フォームに**誕生日入力**（従来は列があるのに常に null 送信＝実質バグ是正）。
   担当キャストの絞り込みチップ。
7. **/report**: 当日サマリの売上カードに前日比（前日が締め済みのときのみ）・組数カードに客単価。
8. **/master/pricing**: 上部に「いま開卓したら」（現在時刻の適用ルールと額・更新ボタン）。
   ルール表下に**重複帯の警告**（重複組の列挙）と「狭い条件を上に」ガイド。冒頭に報酬設定への分離注記。

スキップ項目と理由は `docs/tmp/e8_5_skipped.md`（casts#4 段未割当・席#4 mig 要・staff#1 IA再編ほか）。

## E8-2 日報レーン（採用12件の自己申告表・/report で確認）

| # | 項目 | 実装内容 | 確認手順（demo-manager・CLUB NOX） |
|---|---|---|---|
| #1 | 売上内訳 | 決済ドーナツ（現金/カード/売掛/その他の構成%）＋カテゴリ5バー（セット・延長/ドリンク/シャンパン/ボトル/指名・その他） | 日報タブ・KPI 直下。当日売上があるときのみ表示 |
| #2 | 営業サマリー8指標 | 新規/リピート/本指名/同伴/ボトル開栓/平均滞在/出勤キャスト/値引き（定義注記つき） | 売上内訳の下の帯 |
| #3 | 当日 KPI 5枚 | 売上（前日比）/客単価（組単価）/組数（客数）/現金/カード＝E8-5 先行分に単価系を追補して完成 | 日報タブ先頭 |
| #4 | 現金照合パネル | レジ内現金予定額のライブ表示（釣銭+現金売上+回収現金−諸経費−現金支払の式明示・実査入力で差を即表示） | 締め区画の上 |
| #5 | 金種カウンタ | 9金種（2千円札除外）×枚数→合計→「実査へ反映」 | 締め区画「金種で数える」 |
| #6 | 締めチェック3項目 | 未会計0／決済一致（入金合計=伝票合計）／実査差（未入力は—）。ボトル期限は後送り裁定どおり無し | 締め区画の先頭 |
| #8 | 締め済み日報の列構成 | リッチ行に締め担当併記＋行クリックでその営業日をプレビューへ・15列表に「締め担当」列 | 締め済み日報 |
| #9 | 日別売上推移 | 締め済み daily_reports の日次バー（ホバーで日付・金額） | 月報タブ |
| #10 | キャスト別売上 TOP5 | get_cast_sales の月合算バー（owner/manager のみ） | 月報タブ |
| #11 | 月報 KPI 5枚 | 売上/営業日数/平均日商/売掛残高/前年同月比（前年実績なしは—） | 月報タブ先頭 |
| #12 | 売掛 KPI＋期日管理 | KPI4枚（残高=amount−天引き−回収/今月回収/期限超過/件数）＋期日バッジ（超過=赤・タップで設定/クリアのモーダル=mig0093）＋期日順ソート | 売掛タブ。実機で期日 2026-08-01 設定→「（超過）」赤バッジ→KPI 超過1件を確認済み（検収売掛は void 済み） |
| #13 | 部分回収モーダル | 残額表示・金額入力（空欄=全額）・receivable_collect p_amount 結線・idem は送信ごとに新規 | 売掛行「回収」。表示・キャンセルまで実機確認＝回収実行は段50 の RPC 検証（30本）が担保 |

※#7 締め確認モーダル（PIN）=T5 後送り・#14/#15=現行維持（裁定どおり・不触）。

## E8-3 顧客レーン（採用7件の自己申告表・/customers で確認）

| # | 項目 | 実装内容 | 確認手順（demo-manager・CLUB NOX） |
|---|---|---|---|
| #1 | 担当キャスト絞り込み | **E8-5 実装済み**（チップ）＝今回は突合のみ・変更なし | 一覧上部の担当チップ |
| #2 | 顧客ランク | 一覧に VIP/VVIP バッジ（VVIP=gold・VIP=gold2）＋概要タブに無印/VIP/VVIP セグ（owner/manager のみ・customer_set_grade 結線） | 顧客選択→概要タブ→ランク切替→一覧バッジ即反映（実機で VIP 往復確認済み） |
| #3 | 詳細4タブ | 右ペインを 概要/来店/ボトル/メモ の4タブへ（情報種別は現行と同一の再配置） | 顧客選択で確認 |
| #5 | PROFILE グリッド | 電話・誕生日・担当・最終来店＋来店傾向は「E8-6 で実装予定（T4 従属）」のプレースホルダ明記 | 概要タブ |
| #6 | 好み・接客チップ | prefs を読点/カンマ/空白で分割してチップ表示（書込は現行どおり customer_update） | 概要タブ（prefs のある顧客） |
| #7 | ボトル3列＋編集 | 残量%（≤20 は赤）・棚番号・期限（超過=赤太・14日以内=金）表示＋編集モーダル（残量/期限/棚/状態3値/メモ＝bottle_keep_update・owner/manager） | ボトルタブ→編集。実 UI の保存経路は段51(9)(10) の RPC 検証が担保 |
| #8 | メモ履歴 | customer_notes 一覧（新しい順・記入者名・論理削除除外）＋追記フォーム＋削除（owner/manager）。旧 memo は「旧メモ（読み取り専用・移行しません）」で残置 | メモタブ（実機で追記→表示→削除を一巡済み） |
| #9 | 平均会計 | 概要タブの3stat に平均会計＝total_spend÷visits（RPC 集計値の再形） | 概要タブ |

※#4 NEXT ACTION=後送り・#10 誕生日=E8-5 実施済み・#11=現行維持（裁定どおり）。
※cast には customer_notes が RLS で全面不可視（担当客でも0行）＝段51(14)＋adversarial で実証済み。

## E8-4 シフトレーン（採用7件の自己申告表・/shift ＋ /dashboard で確認）

| # | 項目 | 実装内容 | 確認手順（demo-manager・CLUB NOX） |
|---|---|---|---|
| #1 | 本日のシフト一覧 | 今日タブに当日シフトを開始時刻順で列挙（写真チップ・時間・確定/予定ピル） | 今日タブ（当日シフトなしの日は案内文） |
| #2 | 時間帯別充足バー | staffing_needs のバンド（mig0095）ごとに「交差シフト数/required」のバー＋3色ピル。今日タブ・カレンダー日詳細の両方。日の状態＝バンド最悪値・必要人数＝ピーク（max）・不足＝最悪バンドの不足数（合算は跨ぎ勤務の二重計上になるためしない） | 今日タブ「本日の充足（時間帯別）」。実機で金曜を 16:00〜20:00(2名)/20:00〜24:00(3名) に分割→日詳細にバー2本・ピーク 0/3 を実測→終日5名へ復元済み |
| #3 | バンド編集 UI | シフト作成タブ「必要人数（曜日・時間帯別）」＝曜日ごとのバンドチップ（終日/HH:MM〜HH:MM・人数は onBlur で set_staffing_need 5引数 upsert・×=staffing_need_remove）＋追加フォーム（終日チェック/時刻2欄/人数）。重複は RPC 'overlap' を日本語で表示 | シフト作成タブ。実機で overlap 拒否トースト「他の時間帯と重複しています…」→削除→2バンド追加→復元を一巡済み |
| #4 | 予想人件費の月次ロールアップ | カレンダーカード下部に表示月の forecastDay 合算（fcByDate の再形のみ＝labor-forecast 計算・golden 非干渉）＋概算注記 | カレンダータブ（manager 以上）。実機で「予想人件費（2026年8月合計）¥530,000」表示を実測 |
| #5 | 出勤ボーナスの理由・対象 | 理由入力（≤200字・任意）＋対象トグル（全員=null/選択=cast 複選チップ）→publish route→incentive_publish 7引数。履歴行に「全員/対象N名（title=名前）」と理由を併記。対象∩出勤者の配分は段52(13)＋payroll 133本が担保 | 今日タブ 出勤ボーナス区画（発行は実行せず UI 表示まで＝DB 痕を作らない） |
| #9 | スタッフ別マトリクス | 確定シフトタブに 表示月×キャスト の表（●=確定 ○=予定・title に時間帯・シフトのあるキャストのみ・横スクロール） | 確定シフトタブ。実機で 6行×31日・マーク32個の描画を実測 |
| #10 | 確定者列＋CSV | 一覧に登録者列（created_by→users.name 1クエリ・E8-2 #8 と同流儀）＋「CSV 出力」（表示中一覧を BOM 付き UTF-8・日付/曜日/キャスト/開始/終了/状態/登録者） | 確定シフト・シフト作成タブ。実機で登録者「阿部」表示を実測（CSV はダウンロード動作のみ未実測＝client Blob 生成） |

※dashboard-board も staffing_needs 新構造対応（同じ交差式・ピーク/最悪バンド判定）＝実機で「不足・必要4人・あと4人」（水曜終日4名）を実測。
※#6/#7/#8 ほか非採用項目は E8-4 指示の採用7件外（裁定どおり不触）。

## 検収痕（申告）

- E8-4 実機検収（CLUB NOX 金曜バンド）: 終日5名→削除→20:00〜24:00(3)/16:00〜20:00(2) 追加→
  両削除→**終日5名へ復元済み**（UI で required=5 実測）。金曜行の staffing_needs.id は再作成で変わったが
  (dow, from_min, to_min, required) は原状どおり。この一巡で audit_logs に set_staffing_need×3・
  staffing_need_remove×3 の計6行が残る（データ痕はこれのみ・incentive 発行は実行していない）。
- 検収臨時卓: **無効化済み**（DB 読取確認済み）。
- void 伝票3枚（¥5,500×2・¥7,700）＋ **closed 伝票1枚 ¥2,200（現金入金 預¥5,000/釣¥2,800）**
  = F6 の close 実測に必要だったため残置（void せず closed のまま申告）。
- CLUB NOX の時間単位は**人数単位のまま**（F1 指示による恒久変更・復元しない）。

## DB（mig0091）

- dev 適用済み（Agoora 手貼り・検証3/3緑）・CC 事後照合合格（pay_group CHECK は length 1..20 で
  RPC 層 '^[A-F]$' はその内側＝不整合なし・group_due 任意グループ文字OK・time_auto 拒否は設計妥当）。
- **収蔵済み**: `supabase/migrations/0091_check_line_set_group.sql`（関数ブロック＝live prosrc byte 一致を
  実測・掲出版ファイル未受領のため 0060/0082 方式＝live 機械抽出で収蔵・sha256 `0a5e19bd…2b70c` 3939 bytes）。
  手貼りリストは 0001〜0091 へ更新済み。本番手貼り時は `notify pgrst, 'reload schema';` を別途実行。
