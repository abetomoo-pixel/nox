# 0918_R15_pre — R15 シフト画面の情報設計（設計裁定待ち）の着手前読取（結論なし・裁定は相談役・2026-09-18）

## 台帳本文（逐語）

- L3307（裁定253 E 第 2 期）: 「E 第 2 期: R15 シフト画面の情報設計の見直し（「確定」の語が計画の公開・行の confirmed・確定シフトタブで別の意味に使われ、5 タブを 3 往復する導線になっている。タブ構成の畳み込みを別裁定で設計）。」
- L3313（v32 §6 逐語）: 「…／R15 シフト画面の情報設計(第2期)／…」
- L3314（9/17 の棚）: 「R15＝設計裁定待ち（第2期・裁定270-4）」／L3588（裁定270-4）: 「R11・R19 は第2期に送らず 0148 に同乗。R15 は設計裁定が先（本文は v32 §6）。」
- 関連の既決: 裁定253 C（`a9fd505`）＝「スタッフに公開して確定」→「スタッフに公開する」・仮シフト／シフト作成／確定シフトの 3 タブに「確定する（n 件）」導線（承認待ちタブへ遷移）。裁定251 M1（report-board）・裁定244（左右分割）・裁定108（CastPicker 維持）・裁定245-6（不足 n）。

## 現状の器（読取）

- 画面＝app/(manage)/shift/shift-board.tsx（1 ファイル・約 2,200 行）。タブ 5 本（868〜869）: `today 今日／queue 承認待ち／build シフト作成／calendar 仮シフト／roster 確定シフト`。「確定」の語＝91 箇所。導線: 837「確定する（{unconfirmedN} 件）」→ `setTab("queue")`・1591「スタッフに公開する」→ `setPeriodStatus(cur, "published")`（shift_period_set）。
- 「確定」の 3 義（本文どおり）: ①計画（shift_periods.status＝published＝公開・裁定253 C で語を変更済み）／②行（shifts.status＝confirmed＝承認・shift_cast_confirm／shift_confirm_bulk）／③タブ名「確定シフト」（roster＝confirmed 行の一覧）。
- live 定義（docs/tmp/0918_R15_live.md・逐語）: shift 系 RPC 14 本の署名・md5・gate（A/B）、shifts の列・CHECK・index、shifts.status の distinct、shift_periods／shift_wishes／staffing_needs／shift_rules の列。要点＝shifts.status は CHECK の 3〜4 値（live distinct は 0918_R15_live.md 参照）・period は shift_periods.status で公開を持つ・wish は shift_wishes（cast の希望＝事実記録・B(i)）。

## 既存の器で足りる部分／足りない部分

| 論点 | 足りる（client のみ） | 足りない（mig） |
|---|---|---|
| タブの畳み込み（5→3 等） | ○ タブ配列（868）と各タブの描画分岐は client。状態は URL クエリ／state のみ | — |
| 「確定」の語の統一（公開／承認／確定済み） | ○ ラベル・トースト・見出し（91 箇所の置換・裁定253 C の続き） | — |
| 承認待ち→確定の導線を 1 画面化 | ○ queue と roster を 1 タブの 2 区画にする等（描画のみ） | — |
| 行の状態を増やす（例: 公開済み・未確認・確認済み・欠勤） | — | ★shifts.status の CHECK 拡張＝mig（4 段は status 拡張とセット＝メモリの設計入力）・pin: rls／shift-deep／shift-confirm／staff-shift の status 逐語 pin |
| 期間の状態を増やす（draft／published 以外） | — | ★shift_periods.status CHECK＝mig・pin: shift-deep／shift-modal |
| cast 側 /mine の見え方（確認ボタン＝shift-confirm-button） | ○ 表示のみなら client | 確認の意味を変える（例: 既読）なら shift_cast_confirm の改修＝mig |

## 設計の分岐点（選択肢ごとに mig 要否と pin への影響）

1. **タブ構成**: (a) 5 タブ維持・語だけ統一（mig 0・pin 0）／(b) 3 タブ（今日／作る（作成＋仮）／確定（承認待ち＋確定シフト））＝client のみ・pin 0・ただし shift-modal／shift-confirm の suite が UI 文言を pin していれば張り替え（要 grep）／(c) 1 画面ウィザード化（大・裁定245 系の再設計）。
2. **「確定」の語**: (a) 公開＝スタッフに公開／承認＝行の確認／確定＝roster のみ、の 3 語固定（client）／(b) 「確定」を行の承認に一本化し roster を「シフト表」に改名（client）／(c) 期間の状態を「確定」に寄せる（shift_periods.status 拡張＝mig）。
3. **行の状態（4 段）**: (a) 現状の status のまま（0）／(b) status 拡張（mig＋pin 多数＝rls 2217 系・shift-deep 80・shift-confirm 10・staff-shift 53・anon-guard probe）。
4. **cast 確認の意味**: (a) 現状（confirmed＝承認）／(b) 既読／同意を分ける（mig＝列追加 or status）。

## 相談役に裁定してほしい問い

- Q1 タブは 5→3 に畳むか（(b)）・語は 3 語固定（2-(a)）でよいか。
- Q2 行の状態は現状 3〜4 値のまま（3-(a)）で第 2 期に入れるか、4 段拡張を R15 に同乗させるか（同乗なら mig 1 本＋pin 5 系の張り替え）。
- Q3 cast 側の「確認」は既読と同意を分けるか（4）。
- Q4 R15 の実装は client 1 便（1-(b)＋2-(a)）で先に出し、mig 系は別裁定に切るか。
- Q5 suite: shift-board の文言 pin（shift-modal 22／shift-confirm 10）を先に洗うか（grep 結果は次便）。
