# 0918_pm_decisions — 追加便 H〜L の仮決め（Agoora 不在・保守的な側＝表示を変えない側で仮決め・2026-09-18 午後）

| # | 箇所 | 選んだ側 | 理由 | もう一方 |
|---|---|---|---|---|
| 1 | H M5「2 ペインのモーダル（シフト追加）がスマホで機能しない」の解釈 | **≤899 で 2 ペインを 1 列に積むだけ**（左＝キャスト選択・下＝月カレンダー。900+ は旧 inline grid と同値の 230px＋1fr・gap 12） | 台帳本文（裁定247 M5）と 9/17 調査（shift-add-form 365 の inline grid に @media なし）が指すのは「2 ペインの横並びがスマホで壊れる」こと。表示のみで中身・経路を変えない | CastPicker を縮める／日別時間行（96px＋72px×2）の再配置／ボタン脚の再配置（要デザイン裁定＝しない） |
| 2 | H M5 右ペインの文言「左でキャストを選択してください。」 | **据え置き**（≤899 では実際は「上」） | 文言変更は表示の変更＝保守側。1 列時の文言は裁定で決める | 「上で／左で」を @media で出し分け（span 2 本）または「キャストを選択してください」に中立化 |
| 3 | H M5 の CSS の置き場 | 既存 `.nox-2col` の**修飾子 `.nox-2col--side`**（M17 の `.nox-2col--32` と同型・新トークン 0） | 新クラス体系を作らない・≤899 の 1 列は既存規則に乗る | shift-add-form 専用の inline `<style>`／新クラス |
| 4 | I 表の実数（21 vs 22） | **実数 22 を正**とし、analytics 1291（コホート表）は既に overflowX:auto で包まれているため対象外＝適用 20＋見送り 1 | 裁定251 の「21」は 9/14 の実測・以後の増分は実数優先（I-1 の指示どおり） | 1291 を `.nox-tablewrap plain` に置換して統一（表示は同じ・変更を増やすだけ） |
| 5 | I register-board 2820（会計タブ明細表・表内に Picker） | **見送り** | Picker のポップが容器の overflow で切れる（I-2 の見送り条件） | 容器に `overflow: visible` を条件付与／Picker をポータル化（新 UI 変更） |
| 6 | I AssignTab 割当表（comp-sections 616・表内に native `<select>`） | **適用** | native select のポップは OS 描画＝overflow の影響を受けない | 見送り（select を picker に置換するのは裁定259 の別レーン） |
| 7 | I payroll-board 653（.nox-paytable・width 100%・潰れ型） | 容器のみ・**th nowrap は付けない** | 裁定252 の `.nw` はプレビュー表専用と明記されている | `.nw` を付けて横スクロール化（列見出しの折返しを止める） |
| 8 | I kiosk-register の 2 表 | **適用**（一覧に含まれているため） | タブレット幅では効かない（900+ で溢れないなら無害） | kiosk は対象外として除外 |
| 9 | I `.nox-ptwrap` hidden→auto の効く幅 | 901+ のみ（≤900 は既存のカード積み規則が visible を上書き） | 既存の @media を触らない | ≤900 でも auto にする（カード積みと矛盾＝しない） |
| 10 | I-3 目視の範囲 | verify org（owner-a／cast-a1a）で JS 計測・**溢れる表は出なかった**ことを一覧に明記 | DEMO owner はログイン不可・実機データは Agoora 持ち越し（K-3） | fixture を厚くして溢れさせる（DB を触る＝本便の禁止事項） |
| 11 | K-2 R15 | 結論は書かず分岐と問いのみ | 指示どおり（裁定は相談役） | — |
| 12 | J 赤時の扱い | 設計上の衝突なら push 見送り・コミットは残す | 指示どおり | — |
