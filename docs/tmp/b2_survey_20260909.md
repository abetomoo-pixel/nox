# B2 日報・月報・売掛 v2.1 着手前調査（2026-09-09・読取のみ・実装は裁定後）

前提: 対応表 §8（D1〜D49）。B層＝「準備中」＋「新要件（器あり・UI なし）」のうち mig 不要で読取／表示だけで写せる行。
現行の読取: report-board.tsx＝checks／payments／check_lines／bottle_keeps／attendance／ar_collections（当日 sum・当月 sum）／daily_reports／users／receivables（open）。RPC＝daily_report_close／reclose／receivable_collect（p_method "cash" 固定 :238）／receivable_set_due／receivable_mark_deduct。

## B2 対象候補（器あり・mig 否）

| 行 | 内容 | 器（実測） | RLS（実測） | 新規クエリ | 名簿区分／RPC | 裁定要 |
|---|---|---|---|---|---|---|
| D6 | 営業時間 19:00〜翌5:00 | store_business_hours（0032: dow／is_closed／open_hm／close_hm） | org＋owner∨自店・cast 不可 | 1（dow 7 行 select） | 読取のみ | 表示位置（営業日切替の隣）・定休日表記 |
| D8 | 「現在 → 営業日」 | bizDateOf 純関数（既 import） | — | 0 | — | 否（実装可） |
| D12 | 取消・返金（監査ログあり） | audit_logs action check_void 等（audit-board.tsx:55 で既にビュー） | **owner 限定**（0002:78） | 1（owner のみ count） | 読取のみ | **要**: owner＝件数＋audit-board 導線／manager＝非表示（裁定190 同型） |
| D14 | 売掛 本日発生 件数／¥ | receivables.created_at＋amount（preview.uri は金額のみ） | owner/manager/staff∧can_register | 1（当営業日範囲 select amount） | 読取のみ | 「発生」＝created_at を cutoff で営業日正規化（裁定196 で KPI 追加は許容済み） |
| D15 | 未処理の入出金（日払い・送り代・立替） | advances（status open・advanced_on）／transport（status open・biz_date）・**立替は器なし** | org＋owner∨自店・cast 本人のみ | 2（open 件数＋金額） | 読取のみ（adv_issue 等は呼ばない） | **要**: 表示＝2 種の件数＋金額のみ・立替は出さない・cast 名は出さない |
| D37 | KPI 今月発生 | receivables.created_at 当月 | 同 D14 | 1（当月 select amount） | 読取のみ | D14 と同じ定義（裁定196 許容済み） |
| D40 | フィルタ 期限超過／今月発生／一部回収 | recvs に due・collected_amount あり・created_at は未 select | 同 D14 | 0（既存 select に created_at 列追加） | — | 否（client フィルタ） |
| D42 | 回収方法／前回／入金日 | ar_collections.method／biz_date 保存済み・UI は sum のみ | owner/manager/staff∧can_register | 1（open 行の receivable_id で最終回収） | 読取のみ | 否（表示のみ） |
| D45 | 回収方法の選択 | receivable_collect は cash/card/other 受理（0092: bad method 検証）・UI "cash" 固定 | — | 0 | **清算 RPC の引数値を可変化**（cash のみ理論在高加算＝DB 式 0055:351-353 は不変） | **要（money 隣接＝Fable／相談役）**: UI の p_method を select にするか |
| D47 | 回収履歴リスト | ar_collections 全項目（一覧 UI なし） | 同 D42 | 1（自店・期間・created_by→users） | 読取のみ | **要**: 期間（当月）・件数上限・列（顧客名は open 行で既に開示中の範囲） |
| D49 | 締め確認 3 チェック | ロジック :676-703 に存在 | — | 0 | — | **裁定196 済**（位置だけ寄せる） |

新規クエリ合計＝8（D6 1／D12 1／D14 1／D15 2／D37 1／D42 1／D47 1）＋既存 select の列追加 1（D40）。mig＝0。
想定コミット＝2（B2-a 日報タブ D6/D8/D12/D14/D15/D49・B2-b 売掛タブ D37/D40/D42/D47〔＋D45 は裁定次第〕）。

## B2 対象外（理由）

- 器なし＝C層③／設計書行き: D16 諸経費明細・D20 差異理由＋承認・D23 締め解除 RPC・D24 締め済み日の編集ブロック・D25 reclosed_at/by・D30 月次確定・D35 売掛 OFF トグル（S24）・D38 手動売掛（money-core）・D48 oldest-first 設定。
- 裁定変更要: D29 月報 CSV（裁定12⑥）・D31 確定日数の母数。
- PII ゲート待ち: D39 顧客検索・D41 の範囲拡大分。
- 新要件（UI・既存値の再形）: D4／D10 締めフロー 01〜05 ステップ・D43 伝票を見る（R57 停止中＝据え置き提案）・D22 説明文（現行は「解除なし再集計」＝C層③後に差替。文言だけ現行仕様に合わせるなら A層扱い）。

## 裁定要点（B2 着手前・8 点）

1. D12: owner＝件数＋audit-board 導線／manager＝非表示（audit_logs の owner 限定 RLS は変えない＝裁定190 同型）。
2. D15: advances／transport の open 件数＋金額のみ・立替は器なしのため出さない・cast 名は出さない。
3. D14／D37: 「発生」＝receivables.created_at を cutoff で営業日正規化（bizDateOf）。checks.closed_at 基準にはしない。
4. D45: receivable_collect の p_method を UI で可変化するか（money 隣接。現金照合式は cash のみのまま＝DB 不変）。Fable／相談役判断。
5. D47: 当月・自店・最新 N 件・列＝入金日／方法／金額／顧客／担当／登録者。
6. D6: 表示位置は「営業日切替 翌HH:MM」の隣に「本日 19:00〜翌5:00」・定休日は「定休日」。
7. D22: 説明文は現行仕様（再集計）に据え置き・C層③後に差替。
8. D43: R57 停止中のため据え置き。
