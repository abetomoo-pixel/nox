# 0918_pm2_decisions — 便 P〜U の仮決め（Agoora 不在・表示を変えない側で仮決め・2026-09-18 午後）

| # | 段 | 箇所 | 選んだ側 | 理由 | もう一方 |
|---|---|---|---|---|---|
| 1 | P | 裁定272「追補2」の見出し | 既存の「追補 2（案 Q）」と番号が重なるため「追補（…相談役ブロック『272 追補2』）」として (ii) の文末に追記 | 既収載の番号を書き換えない | 既存を「追補 2a／2b」に改番 |
| 2 | Q | M15 `.nox-seg` | `overflow: hidden auto`（横のみ auto）＋ `> * { flex: 0 0 auto }` | 収まる幅では従来と同じ描画・縦は hidden のまま角丸クリップ維持 | `flex-wrap: wrap`（折返し＝行高が変わる） |
| 3 | Q | M18 sticky-left の地色 | `var(--card)`（.nox-tablewrap の地色と同値） | 既存トークンのみ・新トークン 0 | `--bg2`（sticky th の既存規則と同値だが表地色と差が出る） |
| 4 | R | topbar 左のロゴ | **≤899 のみ表示**（900+ はサイドバー brand があるため CSS で非表示） | 「同じ情報を 2 箇所に出さない」（E2）を保つ＝900+ の見た目を変えない | 900+ でも出す（裁定275「ヘッダ左にロゴ」の字義） |
| 5 | R | ≤899 の topbar ログアウト | CSS で非表示（歯車 Modal に集約）・900+ は従来どおり topbar | 裁定275「右のログアウトは ≤899 で歯車へ集約」の字義 | 両方に出す |
| 6 | R | 在庫リンク | 「その他」＝分析群の末尾に /master/stock（既存画面・URL 不変・manager 以上） | 裁定275「既存画面があればリンク 1 本」 | 店舗群（歯車）側に置く |
| 7 | R | cast の下タブ | groups＝レジ 1 本のまま・歯車（ログアウトのみ）を追加 | topbar のログアウトを ≤899 で隠すため cast の出口を残す | cast は topbar のログアウトを残す（CSS の例外＝複雑化） |
| 8 | R | 歯車のアイコン | 文字 ⚙（U+2699）・aria-label「設定」・Modal 見出し「設定」 | nav-icons.tsx に歯車が無い・新 SVG＝新資産を増やさない | NavIcon に歯車を追加 |
| 9 | R | ≥900 の歯車群 | SideNav の並びは従来どおり最後（店舗群）＝「下部に同群」を満たす・見出し追加なし | 表示を変えない側 | 区切り線／「設定」見出しを足す |
| 10 | S | 旧 5 キー | state・setTab・パネル分岐・深リンクとも据え置き＝tabs.ts の写像だけ被せる | 「既存 5 パネルは中身不触」・深リンク維持 | state を 3 値に作り替える |
| 11 | S | 承認待ちの件数バッジ | 「確定」タブのボタンに丸数字（従来と同形）＋seg「承認待ち」に素の数字 | バッジの見え方を変えない | seg のみに出す |
| 12 | S | 語の統一の線引き | **動詞（行を承認する操作・トースト・確認ダイアログ・title）だけ「承認」**、状態名（確定＝confirmed／未確定／確定シフト／確定時間／確定人時／確定者）・監査ラベル（shift_confirm_bulk「確定」）・DB 値は不変＝置換 15（13 種） | 裁定274「語は 3 語固定（公開／承認／確定）」＝承認は行の操作・確定は結果の状態 | 状態名も「承認済み」に寄せる（状態名の全面改称＝表示が大きく変わる） |
| 13 | S | 「「承認待ち」タブ」の案内文 | 「「確定」タブの承認待ち」へ（3 箇所＝508／1834／2129）・コメント内は不触 | 承認待ちがタブでなく seg になったため | 据え置き（誤案内になる） |
| 14 | S | cast 側（/mine）の目視 | /mine の表示は不変（shift-confirm-button の語は「確認」＝触らない） | 裁定274 は管理画面のタブ・語のみ | /mine の「確認」も「承認」へ（cast の操作は承認ではない） |

## R-3 role 別の口（裁定275 適用後・≤899）

| role | 下タブ（4） | その他 | 歯車 |
|---|---|---|---|
| owner | ホーム／レジ／日報／シフト | キャスト／スタッフ／給与／顧客／分析／領収書／在庫 | マスタ／お知らせ／監査／ご契約／ログアウト |
| manager | ホーム／レジ／日報／シフト | キャスト／スタッフ／給与／顧客／分析／領収書／在庫 | マスタ／お知らせ／ログアウト（監査・ご契約は owner 限定＝従来の role 条件を維持） |
| staff | ホーム／レジ／日報／シフト | 顧客（can_crm のときだけ）＝無ければ「その他」自体を出さない | お知らせ／ログアウト |
| cast（/register のみ） | レジ | （なし） | ログアウト |

900+ は SideNav（群見出し付き・店舗群が最後）＋topbar 右ログアウト＝従来どおり。項目集合・URL・role ゲートは不変。
