# 0149／0150 突合（便 AD・2026-09-18 15:38 JST・起草者を疑う別パス）

script: docs/tmp/q0918_ad.mjs（ROLLBACK 実証・DB 恒久変更 0）／log: docs/tmp/0149_ad_run3.log／json: docs/tmp/0149_ad_result.json

| 段 | 結果 |
|---|---|
| a-1〜a-7 | OK 7（a-7 は関数本文 prosrc 相当で判定。ヘッダの検証クエリ注記に 'billing locked' の文字列があるため初回は誤検知＝判定を本文に限定） |
| b-1〜b-6 | OK（c_wipe 68 手＝w2 逐語・c_load 67・全表 org_id 有・FK 246 本で削除順／投入順違反 0・自己参照 FK＝checks.merged_into のみ） |
| c-1 | OK（残す表を親に持つ FK 110 本） |
| d-1／d-2 | OK |
| e-1〜e-4 | OK（live 245・gated 128・INTERNAL_PROBES 有・md5 控え） |
| f-0〜f-3 | OK（本文 1 tx 通過・proacl service_role のみ・録画 5 枚 5500/17000/52800/12100/8800・payload 27 表 325 行 143,817 B） |
| **f-4 起草どおり** | **NG 173 ms**: `update or delete on table "stores" violates foreign key constraint "memberships_store_id_fkey" on table "memberships"` |
| f-4' 変種（stores も残す＝c_keep 5・wipe 67・load 66） | OK 239 ms |
| f-5〜f-9（変種で続行） | OK（件数一致・stock_logs sale 6＝トリガ再生成・三点一致 5 枚・★7 at 当て直し mismatch 0・demo_reset_at＋audit 1 行） |
| f-10〜f-14 raise | org mismatch／bad table／not demo／bad stock_logs／bad mode・bad payload＝全 5 種 OK |
| f-15／f-16 | wipe→load の 2 回呼び OK |
| f-17 | ROLLBACK 後 snapshot 一致（is_demo 列なし・関数なし・policy 3 本） |
| g-1 | 同 tx 2 回連続 reset('all')＝件数・Σtotal 197,500 不変（112／134 ms） |
| h | 0149 sha256 bfe2512059bc7a905940c0ffd2d0a70021fd89bc11e31940812b5dafaf10469e・201 行・16,857 B／0150 c6637923c687599b320f9ae8608791a4028368c88357466ad60f8dbee974063d・21 行・1,619 B |

## ★起草を止めた点（相談役判断）
1. **stores は消せない**: 残す 4 表（276-2）の memberships.store_id → stores の FK（memberships_store_id_fkey）があるため、w2 の 68 手目 stores の delete が raise。mig ファイルは起草どおり（stores を wipe に含む）のまま未修正。候補＝(a) stores も残す（c_keep 5 表・wipe 67 手・load 66 表＝変種で f-4'〜g まで実証済）／(b) memberships を wipe に含める（残す表を 3 に）／(c) FK を on delete cascade に変更（不採用推奨）。
2. **★7 の結合キー**: stock_logs.sale 行と check_lines の間に列レベルの結合キー（check_line_id 等）が無い。起草は (store_id, product_id, delta=-qty) の partition＋row_number で対応付け。f-8 で mismatch 0 だが同一商品・同数量の行が複数ある場合の対応付けは順序依存（at の相対順は保つ）。
3. a-7 の判定はヘッダ注記の文字列を除いて本文に限定した。

## 終了確認
HEAD d4d66fe＝origin 0 0・未追跡＝docs/tmp／scripts/demo／0149／0150（コミットしない）・dev 3200 稼働（止めない）・DB 恒久変更 0（f-17 snapshot 一致）。
