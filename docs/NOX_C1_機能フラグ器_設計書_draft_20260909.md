# NOX C層① 機能フラグ器 設計書 draft(2026-09-09)

前提: 横断設計書 v1(裁定179〜182)§3 監査・§4 flag。#62(audit_logs.reason)を同梱。
型: 118/113 と同型=draft 突合(CC live 実測)→裁定→v1→live 再 dump→mig。本書は draft=数値・列名は CC 突合で確定。

---

## 1. 範囲
- 新表 `feature_flags`(二層)・関数 `flag_enabled`(読)・`flag_set`(書・owner)・監査 action `flag_toggle`
- `audit_logs.reason text` 追加・`audit_log_write` に `p_reason` 末尾追加(既定 null・既存呼出不変)
- UI: 店舗設定に「機能」タブ(owner のみ表示・4 key の ON/OFF)。他画面の出し分けは C層②③側で `flag_enabled` を呼ぶ
- 対象外: key の追加(裁定)・C層②③本体・retention
  > 要修正（突合 2026-09-09・§1）: UI の置き場「店舗設定に「機能」タブ」は**現行に店舗設定の専用ページが無い**（マスタはハブ `master-board` の群カード＋`/master/system` の 3 アンカー devices／receipts／secrets）。候補＝`/master/system#features`（owner 判定は同 page.tsx の `isOwner` を流用）か、ハブ「店舗・運用」群に owner 限定カード＝§4 と合わせて裁定。

## 2. 表 `feature_flags`

| 列 | 型 | 制約 |
|---|---|---|
| id | uuid | pk default gen_random_uuid() |
| org_id | uuid | not null fk orgs |
| store_id | uuid | null=org 既定 / fk stores |
| key | text | not null・check key in ('staff_shift','reopen_flow','qr_order','notify') |
| enabled | boolean | not null default false |
| updated_at | timestamptz | not null default now()(更新は RPC 内で明示) |
| updated_by | uuid | null |

- unique: `(org_id, key) where store_id is null` / `(org_id, store_id, key) where store_id is not null`(部分 unique 2本)
- 整合 check: store_id が非 null なら stores.org_id = org_id(トリガでなく RPC 内検証)
- RLS: select は同 org の authenticated(`org_id = auth_org_id()`)。insert/update/delete は policy なし(RPC のみ)
- ACL: `revoke all on feature_flags from public, anon, authenticated; grant select to authenticated`
- 初期行: なし(行なし=false=fail-closed)。seed は入れない

## 3. 関数

### 3.1 `flag_enabled(p_key text, p_store_id uuid default null) returns boolean`
- SECURITY DEFINER・`set search_path = public`・null-guard(p_key null → false)
- 解決: 店舗行(p_store_id 指定時)→ org 行 → false
- org は `auth_org_id()`。p_store_id は同 org 検証(違えば false・raise しない=読取関数)
- STABLE・grant execute to authenticated。UI と C層②③ RPC の双方から呼ぶ(RPC 側は off なら raise 'feature_disabled:<key>')

### 3.2 `flag_set(p_key text, p_store_id uuid, p_enabled boolean, p_reason text default null) returns void`
- SECURITY DEFINER・null-guard(p_key/p_enabled null → raise)
- 権限: `auth_role() = 'owner'` のみ(manager 不可)。fail-closed
- key は check 制約に委ねず関数内でも whitelist 検証(raise 'unknown_key')
- upsert(org/store/key)。before/after を `audit_log_write('flag_toggle', target='feature_flags:<key>[:<store_id>]', before, after, p_store_id, p_reason)`
- grant execute to authenticated(内部で owner 判定)

### 3.3 `audit_log_write` 改変(#62)
- `audit_logs` に `reason text null` 追加
- `audit_log_write(p_action, p_target, p_before, p_after, p_store_id, p_reason text default null)`=既存5引数呼出は無変更
  > 突合 OK（2026-09-09）: 現 signature＝`audit_log_write(p_action text, p_target text=null, p_before jsonb=null, p_after jsonb=null, p_store_id uuid=null) returns uuid`・SECURITY DEFINER・search_path=public。呼出 **141 関数すべて位置引数**（名前呼び 0）＝末尾 default 追加で無変更。`_service` は 7 引数（p_org_id, p_actor, p_action, …）・呼出 4 関数。
- `audit_log_write_service` も同様に末尾 `p_reason` 追加
- 引数追加=同名で pronargs が変わる → 旧 signature を `drop function ... (旧引数)` してから create(0134 の型)。A6 名簿は名前不変・pronargs 更新
  > 突合 OK: audit_log_write は「INSERT 列に reason を足す」だけ。actor は kiosk 経由の fallback（0057）あり＝reason 追加に影響なし。audit_logs の index 4 本・policy 1 本（select・owner）は不変。

## 4. UI(店舗設定「機能」タブ・A層 setter mig とは別コミット)
- owner のみタブ表示(manager 以下は非表示・disabled 表示にしない)
  > 要修正（突合・§4）: 「店舗設定」タブ構成は現行に無い（上記 §1 注記）。owner 判定の既存部品＝`getSessionRole()`（page 側）→ `isOwner` prop（`/master/system/page.tsx:39`・`casts/page.tsx` 等の型）。manager 非表示は同 page で分岐可。
- 4 key × [org 既定 / この店舗の上書き] の 2 列トグル。店舗行なし=「既定に従う」
- 切替時に理由入力(任意)→ `flag_set`。成功後 `flag_enabled` で再読込
- 文言: 「機能の公開」/ key 表示名: 黒服シフト・締め解除フロー・QR注文・通知。ローンチ時は staff_shift/reopen_flow のみ表示、qr_order/notify は非表示(器はある)

## 5. verify(f0 新スイート `verify-nox-flags.ts`・想定 +14 assertions)
1. 行なし → flag_enabled=false(4 key)
2. owner が org 既定 ON → true / 店舗行 OFF → false(店舗優先)
3. manager の flag_set → raise(fail-closed)
4. 未知 key → raise
5. 他 org の store_id → false(読)・raise(書)
6. flag_set 後 audit_logs に flag_toggle 1行・before/after 非 null・reason が入る
7. audit_log_write 旧5引数呼出が通る(後方互換)・p_reason 付き呼出で reason 列に入る
8. grants: feature_flags の anon/authenticated 書込 revoke・authenticated select・関数 execute(A6/A7 更新)
  > 要修正（突合・§5-8／§7）: A6 は**商品・料金マスタ 14 本**（12 本は A7 待遇・報酬マスタ）。flag_set（店設定の書込・ゲート済み）は **A8 店設定（13→14 本）** へ、flag_enabled（STABLE 読取・非ゲート）は **B(f) 読取（43→44 本）** へ収載＝対象 113→114・除外 101→102・全数 214→216（`docs/NOX_課金ゲート対象_v1.md`・billing 段47-1 の pin を同時更新）。audit_log_write／_service は名前不変＝本数不動（旧 signature DROP は f0 の別 assert）。
- golden 6値 不変(money 非接触)

## 6. mig 案(1本・冪等・0135 想定)
順: audit_logs.reason 追加 → audit_log_write/ _service 差替(drop 旧) → feature_flags 表+unique+RLS+ACL → flag_enabled → flag_set → grants 再明示
- 影響: audit_log_write は全 RPC が呼ぶ=live 再 dump で呼出箇所(pronargs 依存なし=名前呼び)を確認。既定引数で後方互換
- ロールバック: drop flag_set/flag_enabled/feature_flags・audit_log_write 旧 signature 復元・reason 列は残置可(nullable)

## 7. CC 突合項目(draft → v1)
- audit_log_write / _service の現 signature と呼出箇所数(名前呼びか位置引数か)
- auth_org_id / auth_role の存在と返り型
- stores.org_id の fk・orgs の存在
- 店舗設定画面の現タブ構成と owner 判定の既存部品
- A6 名簿の現行数(12)・区分(A/B)への追記位置

## 7'. 突合（2026-09-09・CC live 実測）

| 項目 | 実測 | 判定 |
|---|---|---|
| a) audit_log_write | 5 引数（p_action, p_target=null, p_before=null, p_after=null, p_store_id=null）・returns uuid・SECURITY DEFINER・VOLATILE・search_path=public。INSERT 列＝org_id/store_id/actor_user_id/action/target/before_json/after_json/ip | OK＝末尾 `p_reason text default null` 追加で既存呼出無変更 |
| a) audit_log_write_service | 7 引数（p_org_id, p_actor, p_action, p_target=null, p_before=null, p_after=null, p_store_id=null）・returns uuid・SECURITY DEFINER | OK＝同様に末尾追加 |
| a) 呼出箇所 | public 関数 214 本中 **141 本が audit_log_write を位置引数で呼ぶ**（名前呼び 0）・`_service` 呼出 4 本 | OK（§6 の想定どおり名前呼び依存なし） |
| b) auth_org_id / auth_role / auth_store_id | uuid／text／uuid・引数なし・SECURITY DEFINER・STABLE・search_path=public | OK |
| c) stores.org_id fk / orgs pk / uuid | `stores_org_id_fkey → orgs(id)`・`orgs_pkey (id)`・gen_random_uuid() 可（PG 17.6・pgcrypto 1.3） | OK |
| d) 店舗設定画面 | **専用ページなし**。マスタ＝ハブ `master-board.tsx`（群「店舗・運用」＝席・卓／営業時間／キオスク端末／レシート／機密・税務＝`/master/system#devices|receipts|secrets`）。owner 判定＝`getSessionRole()`→`isOwner`（`/master/system/page.tsx:39`）。v3 の「利用機能」カード S4 は N3 で「作らない（C層①）」 | **要修正**（§1／§4 の置き場を裁定） |
| e) A6 名簿 | `docs/NOX_課金ゲート対象_v1.md`: A6＝商品・料金マスタ **14 本**（A7＝待遇・報酬 12 本・A8＝店設定 13 本）。対象 113・除外 101・全数 214（billing 段47-1 pin） | **要修正**（flag_set→A8・flag_enabled→B(f)） |
| f) feature/flag 名 | 表 0/65・関数 0/214・列 0/822 | OK（衝突なし） |

**v1 化への申し送り**: §1／§4 の UI 置き場（`/master/system#features` 推奨＝owner 判定部品が同 page にある）・§5-8／§7 の名簿区分（A8／B(f)）・§3.3 は突合どおりで可。mig 起草（0135・Fable）は本 draft の v1 化後。

## 8. 裁定候補
- C①-1 key の表示名4つ(上記)
- C①-2 ローンチ時に UI へ出す key(staff_shift/reopen_flow のみ)
- C①-3 flag_set の理由入力=任意(監査10種表では flag_toggle は任意)
- C①-4 mig 番号 0135・1本にまとめる(#62 同梱)か2本に分けるか(推奨: 1本)
