# NOX 夜間無人レーン O 朝報告（2026-09-07 夜 → 09-08 朝）

原則どおり push なし・mig なし・手貼りなし。裁定 168〜178 は**無人確定・翌朝承認待ち**（台帳に付記済み）。
origin/main＝`997b37a`。ローカル HEAD＝本書コミットの直前＝`dcef083`（**ahead 6**＋本書で 7）。

## 0. 結論（先に読む）

- O2 お知らせ・O3 給与は**実装とコミットまで完了**したが、**f0 の 2 連緑は未達**（下記 4）。翌朝に f0 を再走して取り直す。
- 夜間後半に **PostgREST 経路だけが極端に遅くなった**（direct pg 9ms に対し signin 13.8s／rpc 22.6s）。DB 直結は正常・長時間クエリなし・接続 22/60。
  日中 30 走超の f0 負荷後の pooler／API コンテナ側の劣化が疑われる。**無人では再走ループしない**規則に従い停止。
- #58 は**クローズしない**（観察継続）。

## 1. 各レーンの結果

| レーン | sha | 変更 | gate | f0 |
|---|---|---|---|---|
| O1 `.gitattributes` | （未コミット・案は scratchpad 退避） | — | — | — → **停止リスト**（下記 3） |
| O1' 教訓61/62 | `ae00521` | 台帳 +16 | — | — |
| O2 お知らせ v2.1 | `2fb16d3` | notices-board.tsx +14/−2 | tsc 緑／lint 緑／ui-tokens baseline 56 不変／eol i/lf w/lf | run1 **緑** 37本 3,550（416s）／run2 赤（pricing 段 null.id・単独再走緑 150）／run3 赤（pricing 段43(18)d statement timeout・936s）＝**2連緑未達** |
| O3 給与 v2.1 | `ac73128` | payroll-board.tsx +23/−4 | tsc 緑／lint 緑／ui-tokens baseline 56 不変／eol i/lf w/lf | run1 赤（anon-guard 段 null.id・1,183s）／run2 sweep が pooler 接続取得不能（ECHECKOUTTIMEOUT 15s）＝**未検証** |
| docs（O2/O3/O4） | `dcef083` | 台帳（裁定168〜178・125 実施記録・#58 追記）・対応表 §11 14 行／§13 23 行 | — | — |
| O5 本書 | （本コミット） | docs/handoff/NOX_夜間O_20260907.md | — | — |

golden 6 値（5931／125802／55233／64／64／53）は緑走・赤走とも到達した段では不変。

## 2. 裁定 168〜178（無人確定・翌朝承認待ち）

| # | 面 | 内容 |
|---|---|---|
| 168 | お知らせ N32 | テンプレートをモックの 5 種へ（仮シフト公開／シフト確定を追加・ラベル逐語）＝定数のみ |
| 169 | お知らせ N1 | lead は LINE／メール（凍結）の語を含むため据え置き |
| 170 | お知らせ N29/N30 | 「今すぐ掲載」は据え置き（配信＝掲示の読み替え裁定済み・語割れ回避） |
| 171 | お知らせ N9 | 対象注記は据え置き（「休店」＝第2期の語） |
| 172 | 給与 W1 | lead「出勤」→「勤怠」のみ（「明細公開」は裁定99-⑦ 後送りの語） |
| 173 | 給与 W7 | 「プレビュー」は据え置き（「再集計」へ改名しない） |
| 174 | 給与 W10 | KPI 4 枚のラベル据え置き（既払額 W11 の定義変更前） |
| 175 | 給与 W22 | 税区分チップ（全員／雇用／委託）＝client 絞り込みのみ |
| 176 | 給与 W23 | 「日数」列＝PayResult.wdays の件数表示のみ |
| 177 | 給与 W2/W12 | 要確認の位置・5 タブ化は据え置き（再配置＝無人外） |
| 178 | 給与 W37/W39/W40 | 税務系 3 行は除外（税理士ゲート） |

## 3. 停止リスト（理由つき）

1. **O1 `.gitattributes`** … `git add --renormalize .` で対象外の `mock/pages-2026-08/nox-pricing-settings.html`（index CRLF／worktree LF の既存不一致）が 191/191 の差分となったため規則どおり着手せず。案（`*.ts *.tsx *.css *.md *.json *.sql text eol=lf`）は scratchpad `gitattributes.proposed` に退避。worktree は `git checkout --` で index に戻し status クリーン。翌朝＝当該 html の改行を確定（LF へ再コミット or 除外）してから再試行。
2. **O2/O3 の f0 2 連緑** … 上記 1 のとおり PostgREST 経路の遅延で取り直し不能。翌朝に `npm run verify:f0` を 2 走（O2/O3 は UI のみで DB 非接触＝赤は環境起因）。
3. **給与 W13/W15（集計元の状態表示）** … preview 応答に取込状態が無く新規クエリが要る（規則 d）＝停止。対応表に「表示欠落・停止リスト」と記載。
4. **給与 W7「再集計」／お知らせ N29・N30「今すぐ配信」** … 文言だけ変えると周辺の語と割れる＝規則 f（裁定173／170 で据え置き・翌朝判断）。

## 4. #58 観察（O4）

sweep 後の面別 f0（キャスト／日報／ホーム／分析／お知らせ run1）は **timeout 0・9 走緑**だったが、夜間後半にお知らせ run3（statement timeout）・給与 run1（null.id＝error 未捕捉の派生）・run2（pooler 接続取得不能）が連続＝**クローズせず観察継続**。
新材料＝**PostgREST 経路のみの遅延**（DB 直結 9ms・pg_stat_activity に長時間クエリなし・接続 22/60）。翌朝の再走で回復していれば「日中負荷後の API 層劣化」として #58 に追記、回復していなければ Supabase 側（pooler／compute）の確認へ。

## 5. 対応表の更新行

- §11 お知らせ（14 行）: N1／N9／N29／N30＝実装済(据え置き・裁定169/171/170)、N32＝実装済(A層・裁定168)、他 9 行＝変更なし。
- §13 給与（23 行）: W1／W22／W23＝実装済(A層・裁定172/175/176)、W2／W7／W10／W12＝据え置き（裁定173/174/177）、W13／W15＝表示欠落・停止リスト、W37／W39／W40＝税務凍結・除外（裁定178）、他＝変更なし。

## 6. 翌朝の目視対象

| 画面 | 項目 |
|---|---|
| `/notices` | 作成フォームの定型文セレクト（5 種・ラベル「全体ミーティング」「給与明細公開」「仮シフト公開」「シフト確定」）と流し込み本文 |
| `/payroll` | hero lead「勤怠・売上・…」／プレビュー表の絞り込みチップ（全員／雇用／委託）／「日数」列（プレビュー後に N日） |

## 7. 起動時の確認（翌朝ブロック用）

1. `git status`（docs/tmp 残置のみ）／`git rev-list --left-right --count origin/main...HEAD`（0 7 期待）
2. `npm run verify:f0` ×2（37 本 3,550・golden 不変・所要が 7〜12 分に戻っているか）
3. 回復していれば O2/O3 の実施記録へ f0 結果を追記→push 判断。回復していなければ #58 へ環境側の記録を追加。
4. O1 の再試行可否（html の改行確定）。
